name: Test Scenario

run:
  requests: 10
  concurrency: 2
  timeout: 5000

request:
  method: POST
  url: http://localhost:8000/test

headers:
  Authorization: Bearer abc
  Idempotency-Key: "{{uuid}}"

body:
  key: value
  amount: 100
